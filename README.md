# clash-yaml-demo

可视化编辑并生成 Clash YAML 的两段配置：

- `proxy-providers`
- `proxy-groups`

支持：
- 可视化编辑
- 导出 YAML

## 本地运行

直接双击 `index.html` 或使用任意静态服务器。

例如：

```bash
python3 -m http.server 8080
```

打开 `http://localhost:8080`